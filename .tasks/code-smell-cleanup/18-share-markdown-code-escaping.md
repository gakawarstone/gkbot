# 18: Share Markdown code escaping

**What to build:** Use one HTML-escaping function for fenced and inline code while preserving their distinct wrapper markup.

**Blocked by:** None (can start immediately).

**Status:** ready

- [ ] Ampersands, angle brackets, and asterisks are escaped by one implementation.
- [ ] Fenced-code language handling and inline-code output remain unchanged.
- [ ] Tests cover both code forms and already special input characters.
